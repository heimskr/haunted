#ifndef UI_LOGSTREAM_H_
#define UI_LOGSTREAM_H_

#include <memory>
#include "ui/ui.h"

namespace spjalla {
	class logstream {
		private:
			// ui &parent;

		public:
			// logstream(ui &ui_): parent(ui_) {}
	};
}

#endif
